const pool = require('../db');
const bcrypt = require('bcrypt');

async function updatePassword() {
    let conn;
    try {

    }
    finally {
        if (conn)
            conn.release();
    }
}

module.exports