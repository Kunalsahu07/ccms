# Backend API Documentation

## Swagger UI

After starting the backend server, open:

`http://localhost:5000/api-docs`

This page shows the generated Swagger documentation for all backend routes.

## Start server

```bash
cd backend
npm run start
```

If the root project uses a different script, run the relevant backend start command from the `backend` folder.
